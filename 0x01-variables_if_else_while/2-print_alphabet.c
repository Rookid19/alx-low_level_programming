#include <stdio.h>

/**
 * main - print string
 *
 * Description: use printf to print string
 *
 * Return: 0 for success
 *
 */

int main(void)
{
	char alphabet = 'a';

	while (alphabet != '{')
	{
		putchar(a);
		alphabet++;
	}
	putchar('\n');
	return (0);
}
