#include "main.h"

/**
* _abs - converts integer n to a positive number
*
* @n: number
*
* Return: absolute value
*/

int _abs(int n)
	{
	if (n >= 0)
	{
	return (n);
	}
	else
	{
	return (n * -1);
	}
}
